

model User {
  id       Int    @id @default(autoincrement())
  firstName     String
  lastName    String
  email    String  @unique
  posts    Post[]
  password String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
